# GitHub APK build
Upload the project contents to the repository root.
Keep `.github/workflows/build-apk.yml`.
Then use Actions → Build RusPro AI APK → Run workflow.
Download artifact: `RusProAI-debug-APK`.
